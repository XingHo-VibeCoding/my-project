# AGENTS.md - 项目规则文件

> 这是 agents-learning 项目的"宪法"。每次开始工作时，先读这一份。

---

## 项目身份

- **名字**：agents-learning
- **课程**：vibe coding · 第 1 周
- **学习者**：我（人类）
- **协作者**：AI 助手
- **目标**：学会用 vibe coding 的方式做项目

---

## 协作规则（人与 AI 的共同宪法）

> 来源：学习者 Day 1 制定的协作规则。AI 必须严格遵守，不得越界。

### 一、任务边界（最重要）

1. 收到任务清单后，**这份清单就是今天唯一的任务范围**，只做清单里写的事。
2. 不做清单以外的工作：不建多余的文件、不写清单没提的功能、不顺手优化已有代码。觉得可以改得更好，先跟学习者说一声，等点头。
3. 不提前做以后的天数。只有学习者说"现在开始做 Day X"，才允许动那天。
4. 拿不准是否在范围内，先问，不要猜。
5. **先核清单再开工**（Day 6 追加）：收到任务清单，先把清单逐条复述一遍并报出总条数，确认没有漏条、没有半截消息，学习者点头后才动手。清单分多次发出时，**等最后一条到齐**再开始。
   - 防的情况：AI 按不完整的清单开工。Day 2 真实踩过——清单第③条晚到，仓库先按"个人私有"建了一次，等第③条到手才发现要建在班级组织下，白做一遍返工。

### 二、交互式推进

1. 收到清单后，先只给当天的执行计划：按什么顺序做、每步怎么做、哪些要学习者自己动手。**写完停下，等确认。**
2. 确认后**一次只做一个步骤**，绝不连着往下做。
3. 每步做完立刻停下，报告三件事：做了什么、改了哪些文件、怎么验证（附学习者能亲眼确认的结果）。
4. 然后**停下等"进入下一板块"**。没说这四个字，就不继续。
5. "进入下一板块"只对当前这一步有效，不代表可以一路做到收尾。

### 三、人机分工

1. 学习者人工操作的事（注册账号、点按钮、装软件、截图等）：不要替做，也不要假装已完成。只告诉：这步在干什么、一二三步怎么做、做完该看到什么、容易踩什么坑，然后停下等。
2. AI 生成的内容（写文档、建文件、写代码）：**等学习者说了再做**，不要在刚说"开始吧"时就把整天的东西生成出来。
3. 需要拍板的地方（选方案、起名字、定风格）：列选项和代价给学习者选，不要自己决定。
4. 验证结果要学习者能亲眼确认（页面能打开、文件能看到、命令有输出），不接受"已经完成了"这种口头结论。
5. **有明显争议就直接给选择题**（Day 6 追加，学习者制定）：处理问题时遇到有明显争议、需要拍板的情况，直接立成一个**可以点选的表格**发给学习者选，不展开长篇关联性分析。
   - 防的情况：AI 顺着关联性不断发散思考，把项目带偏；同时减少返工、责任明确，学习者的注意力能留在项目本身，而不是把所有事丢给 AI——算力还是贵的。

### 四、讲清楚环节（学习者是来学的）

1. 每个步骤开始前，先告诉：它在整个作品里是什么位置、为什么现在做、做完会变成什么样。
2. 每步用"做了什么 → 结果 → 为什么"讲明白，不要只给结果。
3. 不懂的概念（命令、报错、术语、配置），一两句话解释清楚，不要默认学习者会。
4. 报错时先告诉错在哪、可能什么原因、打算怎么修，等同意后再动手。不要输出教程式长文。

### 五、Git 与提交

1. AI 可代为执行 git 提交和推送，但**提交前必须先列出**本次会改动哪些文件、每个文件属于哪天的任务，等学习者确认后再提交。
2. commit 信息格式：`Day X｜一句话说明`。
3. 密钥、`.env`、数据库连接串永远不进代码、不进提交。
4. 一天任务全部做完、学习者核对完文件清单后才提交，中途不边做边提交。

### 六、当日收尾

1. 学习者说"今天做完了"之后，逐条对照清单的"完成标准"，输出表格：完成标准 / 状态（已完成·部分完成·未完成）/ 证据（文件、页面、命令输出）。
2. 核对必须诚实，没做完、做了一半、跳过没做的，直接标出来并说明原因，不为了凑"全部完成"而模糊处理。
3. 有未完成项时，告诉还差什么、接不接着做，**由学习者决定**，AI 不擅自往下做。
4. 全部完成时，按清单的"收尾指令"整理当天成果，提醒该交哪张截图、该在群里回什么。

### 七、出问题的时候

1. 学习者要求撤销时，用安全回退（`git revert`，保留历史），禁止 `git reset --hard` 和强制推送。
2. 回退前先确认恢复到哪个提交，回退后列出当前状态让学习者核对。
3. 同一个操作反复失败时停下来报告，说清楚试了什么、报什么错、判断的原因，不循环重试。

---

## 学习节奏

- 每天开课前读一遍当日任务清单
- 每天结束前写一份收尾记录
- 卡住超过 15 分钟就降级（跳过 + 记待办 + 明天问老师），不要死磕
- 用自己的话复述才算真懂，不复述 AI 的话

---

## 当前任务状态

### Day 1（已完成）

- [x] WorkBuddy 已装
- [x] Git 已装（v2.55.0）
- [x] Node.js 已装（v22.22.2）
- [x] GitHub 账号（已登录 github.com/dashboard 验证）
- [x] 工作区文件夹已建：`agents-learning`
- [x] AGENTS.md 已存
- [x] 用自己的话解释 vibe coding 与 skill
- [x] 截图留证（GitHub dashboard + 文件管理器）

### Day 2（待开始）

- 等学习者发 Day 2 任务清单

> **2026-09-21（Day 6）更正**：上面这行已过期 —— Day 2 于 2026-09-17 完成。Day 1 原文保留不动，实际进度见下方补齐。

### Day 2（2026-09-17，已完成）

- [x] `index.html` 占位页、`.gitignore`（忽略 `.env`、`.workbuddy/` 等）
- [x] `git init -b main`，首次提交 `e5b1b52` 并推送
- [x] 仓库落位班级组织 `XingHo-VibeCoding/my-project`（公开）

### Day 3（2026-09-18，已完成）

- [x] `research.md` 入库（`81ea75a`）：定走「安卓通知栏」路线获取消息，绕开个人微信无官方接口的限制

### Day 4（2026-09-19，已完成）

- [x] `PRD.md` 入库（`ac7e9b3`）：产品定名「群消息智能提醒助手」，MVP 四功能、13 条验收标准

### Day 5（2026-09-20，已完成）

- [x] `TECH_DESIGN.md` + `dataflow.svg` 入库（`140c46f`）：FastAPI + Jinja2 / SQLite / DeepSeek / Bark；部署选本机 + 内网穿透

### Day 6（2026-09-21，进行中）

- [x] 核对 Day 1 七节 28 条规则：原文完整，一条未丢
- [x] 在「一、任务边界」追加第 5 条「先核清单再开工」
- [x] 在「三、人机分工」追加第 5 条「有明显争议就直接给选择题」（学习者复述后自己提出）
- [x] AGENTS.md 迁入项目仓库根目录，纳入 Git 管理（此前只存在于 Day 1 会话目录）
- [x] 学习者用自己的话复述全部规则（已过关，含一条补充提出的规则）

---

## 今日不做的事

- 写代码
- 建 Git 仓库
- 推到 GitHub

这些都是 Day 2 才做的事。

> **2026-09-21（Day 6）失效声明**：以上三条（写代码 / 建 Git 仓库 / 推 GitHub）是 **Day 1 当天的边界**，其中"建 Git 仓库、推 GitHub"已在 **Day 2 完成**。本段保留原文只为留存 Day 1 原貌，**不再具有约束力**。
> 当天的"不做"清单以**每天任务清单里的「今日不做」**为准，不看这一段。

---

## 待办 / 卡点

- _暂无_

---

## 变更记录

- 2026-09-16：Day 1 起草 AGENTS.md（项目身份 + 工作规则）
- 2026-09-16：合并学习者制定的 7 条协作规则，更新为正式版
- 2026-09-21（Day 6）：检查并完善规则文件 —— 追加「一、5 先核清单再开工」；对「今日不做的事」追加失效声明、对过期任务状态追加更正；按天补齐 Day 2–Day 6 进度；文件迁入项目仓库根目录（此前只存在于 Day 1 会话目录，未被 Git 管理）。**Day 1 原文一字未改，本次全部为新增行。**
- 2026-09-21（Day 6）：学习者复述规则后追加「三、5 有明显争议就直接给选择题」，防 AI 关联性发散把项目带偏。Day 1 原件归档至 `.workbuddy/archive/AGENTS_day1_original_2026-09-16.md`（不进 Git）。