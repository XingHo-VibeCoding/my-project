# PRD — 群消息智能提醒助手

> Day 4｜第 1 周｜Vibe Coding 训练营
> 本文件回答一个问题：**我要做的东西长什么样、做成什么样算完成**。
> 依据：`research.md`（Day 3 结论）。本文件不做技术选型（Day 5）。

---

## 一、背景与目标

- **痛点**：微信群消息量大，手动刷消息累，分不清重点与紧急信息，重要事项被闲聊淹没。
- **一句话定位**：把刷屏的群消息，降噪成**推送到手机的精选提醒**和**一条按截止时间排好的待办时间线**。
- **产品名说明**：不叫「全自动」，因为消息来源依赖用户配置现成转发工具；「智能提醒」站得住，因为判断这一步由 AI 完成。
- **成功的判定**：重要消息不漏（该提醒的都提醒了）、闲聊不打扰（不该提醒的一条都不推）。

## 二、用户与场景

- **目标用户**：需要盯群消息、容易被海量闲聊淹没、怕错过重要事项的人（学习打卡群、工作对接群）。**首个用户是我自己。**
- **典型场景**：
  1. 打卡群 200 条消息里混着一条「周五 22:00 前交作业」——原生通知每条都响、已被无视，本产品只在真正重要时响一次。
  2. 工作对接群里埋着一条规则变更/紧急通知，用户没空刷屏，事后从手机推送里直接看到要点。
- **使用前提**（用户侧，不属开发范围）：
  - 安卓手机（iOS 不支持读取其他 App 通知，本产品不支持 iPhone）
  - 手机安装并配置 SmsForwarder（开源现成工具，负责捕获微信通知并转发）
  - 电脑保持程序运行（接收转发的通知并做 AI 判断）

## 三、功能范围

**整体链路**（组件间的分工，不是要开发的全部）：

```
微信群消息 → 安卓通知栏 → SmsForwarder（现成，转发）→ 【本产品】接收 + AI 判断 → 手机推送提醒 + 待办时间线
```

真正要开发的只有方括号内两段。MVP 共 4 个功能：

| # | 功能 | 一句话 |
|---|---|---|
| F1 | 消息接收与解析 | 接住转发的通知，解析出群名/发送人/内容/时间 |
| F2 | AI 重要性判断 | 语义区分「重要待办 / 一般 / 闲聊」，提取要点和截止时间 |
| F3 | 精选推送 | 只把重要的推送到手机，闲聊一条不推 |
| F4 | 待办时间线 | 带截止时间的项排成一条可随时查看的时间线 |

## 四、功能详情与验收标准

> 验收标准统一用「给定…，当…，那么…」写法。Day 7 验收时逐条对照，做不到就是没完成，与感觉无关。

### F1 消息接收与解析

**描述**：程序提供接收端点，接住 SmsForwarder 转发的微信通知，解析并留存为消息记录。

**验收标准**：
1. 给定一条含群名、发送人、内容、时间的转发通知，当程序接收成功后，那么消息列表新增一条记录，四个字段无缺失。
2. 给定连续到达的 20 条通知，当全部接收后，那么消息列表有 20 条记录，顺序与到达顺序一致。
3. 给定一条格式异常/不完整的转发内容，当程序接收后，那么程序不崩溃，并记录该条为「解析失败」，不影响后续消息接收。

### F2 AI 重要性判断

**描述**：对消息记录做语义判断，输出三类：**重要**（含任务/截止时间/紧急/@我）、**一般**（有价值但无需行动）、**闲聊**（含纯表情）；重要项提取一句话要点和截止时间（若有）。

**验收标准**：
1. 给定附录 A 的 12 条混合群聊记录（4 条有效 + 8 条闲聊/表情），当程序处理完成后，那么输出包含全部 4 条重要项，且不包含纯表情和纯闲聊。
2. 给定一条带截止时间的任务消息（如「周五 22:00 前交作业」），当处理完成后，那么该项的截止时间被正确提取并可被 F4 使用。
3. 给定一批 12 条消息，当程序开始处理，那么 1 分钟内输出全部判断结果。

### F3 精选推送（提醒出口：手机）

**描述**：仅对判定为**重要**的消息，推送到用户手机；推送通道（如微信服务号推送类服务）属技术选型，Day 5 确定。与原生微信通知的差别：**原生通知每条都响，本产品只推精选**。

**验收标准**：
1. 给定一批含 4 条重要、8 条闲聊的消息，当处理完成后，那么手机只收到 4 条推送，闲聊 0 条。
2. 给定一条判定为重要的消息，当判定完成，那么 1 分钟内手机收到推送。
3. 给定任意一条手机收到的推送，那么不打开电脑、不看原群的情况下，仅凭推送内容（群名 + 发送人 + 要点）能知道发生了什么事。

### F4 待办时间线

**描述**：把带截止时间的重要项整理成时间线界面（简版：按截止时间排序的清单），可随时打开查看；每项旁显示**距截止的剩余时间**（如「剩 2 天 4 小时」），无截止时间的重要项单独分组。

**验收标准**：
1. 给定 3 条截止时间不同的任务，当处理完成后，那么时间线按截止时间升序展示这 3 项。
2. 给定 1 条无截止时间的重要消息，当处理完成后，那么它出现在「无时限」分组，不混入带时间的排序。
3. 给定已有待办数据，当用户打开时间线页面，那么 3 秒内能看到当前全部待办。
4. 给定一项截止时间在未来某刻的任务，当时间线展示时，那么该项旁显示距截止的剩余时间，且随时间推移数值变小。

## 五、本期不做与理由（砍功能清单）

> 每砍一个都要能归入三类之一：**墙砍**（研究证明走不通）、**范围砍**（能做但不属于本期最小切片）、**价值砍**（做了也没人用，已被别的功能覆盖）。

### 墙砍 —— research.md 已证明走不通，不砍就死

1. **读微信进程类方案**（Hook / iPad 协议 / UI 自动化 / 数据库解密）：封号 + 法律风险。
2. **电脑端读取微信消息**：微信 PC 不接系统通知，UI 自动化又被 4.0 封死（wxauto 已归档）。
3. **iOS 端**：系统不允许读取其他 App 的通知。
4. **云端代收微信**：需要交出账号凭证，隐私风险不可接受。

### 范围砍 —— 能做，但不进本期最小切片

5. **自动回复 / 代发消息**：只做「提醒你」，不代替你说话；且是独立的另一套能力。
6. **多用户 / 账号体系 / 权限**：单机自用，MVP 不需要。
7. **消息全文存档与检索**：本期只做「当次判断 + 输出」，不建历史库。
8. **完整日历集成**（导出 .ics / 同步系统日历）：简版时间线先行，集成留后续版本。

### 价值砍 —— 做了也没人用，已被覆盖

9. **关键词规则配置界面**：AI 语义判断是核心，再让用户手动配关键词黑白名单就是多余的第二套系统。
10. **自研手机 App**：通知捕获用现成的 SmsForwarder，重写一遍零价值。

## 六、与 research.md 的衔接 / 待核实项

- 消息来源路线 = research.md 第五节结论（安卓通知栏路线），不再重开研究。
- 待核实项（不阻塞 PRD，但 Day 7 前需实测）：
  1. 微信通知内容的长度上限（受「通知显示消息详情」设置影响）——影响 F1 解析完整度
  2. SmsForwarder 在目标机型上的保活稳定性——影响整体链路可用性
- 用户已拍板：产品名「群消息智能提醒助手」；提醒出口手机；MVP 含简版时间线（2026-09-19）。

---

## 附录 A：F2 验收测试数据（12 条混合群聊记录）

> Day 3 现场演示所用数据的落盘版本（学习打卡群场景）。**加粗**的 4 条为有效项，其余 8 条为闲聊/表情，验证时应被滤掉。

| # | 发送人 | 内容 | 有效？ |
|---|---|---|---|
| 1 | 小王 | 哈哈哈哈今天好困 | ✗ |
| 2 | 小李 | [表情] | ✗ |
| 3 | 班长 | **@所有人 本周五 22:00 前把作业提交到群里，过时不候** | ✓ 带截止时间 |
| 4 | 小张 | 中午吃啥 | ✗ |
| 5 | 小王 | 吃食堂呗 | ✗ |
| 6 | 老师 | **@我 你上次问的那道题，答案我发你私信了，记得看** | ✓ @我 |
| 7 | 小李 | [表情] [表情] | ✗ |
| 8 | 班长 | **提醒：从下周起打卡时间改到每晚 21:00，别记错** | ✓ 规则变更 |
| 9 | 小张 | 周末有人打球吗 | ✗ |
| 10 | 小王 | 我我我 | ✗ |
| 11 | 老师 | **@所有人 明晚 20:00 线上答疑会，链接稍后发** | ✓ 紧急通知 |
| 12 | 小李 | 打球+1 | ✗ |

**预期结果**：输出恰好包含第 3、6、8、11 条；其中第 3 条的截止时间（本周五 22:00）应被提取并进入时间线。
